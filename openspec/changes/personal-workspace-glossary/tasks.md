# Tasks: Two-Tier Glossary (Upstream)

- [ ] Wait for romaco-scriptor `personal-workspace-glossary` to land.
- [ ] Execute `upstream-translation-glossary` against the two-tier
      state (its file inventory then already contains `user_id`,
      scope-aware API routes and the split settings UI).
- [ ] Port the two-tier tests (merge precedence, personal isolation,
      TM-skip, permission enforcement).
- [ ] Definition of Done mirrors the Romaco spec's DoD.
