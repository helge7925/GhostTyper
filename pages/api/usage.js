// Legacy organization-usage URL retained for existing bookmarks. It now has
// the same admin/owner authorization as the canonical organization endpoint.
export { default } from './organizations/usage';
