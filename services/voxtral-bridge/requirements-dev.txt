# Test-only dependencies — never installed into the production Docker
# image (see Dockerfile, which installs only requirements.txt). Install
# both files locally to run the test suite:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
